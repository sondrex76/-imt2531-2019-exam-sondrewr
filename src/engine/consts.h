#pragma once

#include <glm/ext/matrix_transform.hpp>
#include <string>

const int NUM_TRIANGLES_TERRAIN = 2 * 100^2;	// Number of triangles to use in terrain generation, 100 is the current size in squares
const float SIZE_TERRAIN_PIERCES = 10;			// The size of triangles



#pragma once

#include <glm/ext/matrix_transform.hpp>
#include <string>

const int SIZE_ENVIORMENT = 100;				// Number of points
const float SIZE_TERRAIN = 10;					// The size of triangles
const float HEIGHT_TERRAIN = 1;					// Height of terrain per level


#pragma once

#include <glm/ext/matrix_transform.hpp>
#include <string>

const int SIZE_ENVIORMENT = 100;				// Number of points
const int SIZE_ZONES = 10;						// How many zones are SIZE_ENVIORMENT divided into
const float SIZE_TERRAIN = 30.0f;				// The size of triangles
const float HEIGHT_TERRAIN = 1.0f;				// Height of terrain per level
const float MAX_HEIGHT = 10.0f;				// Maximum height
